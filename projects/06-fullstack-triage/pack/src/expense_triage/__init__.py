"""expense-triage: the pack half of a Tier 6 fullstack example. Three commands
(list, classify, approve) that a React dashboard consumes. Deterministic rules do
the work; the model is called only when the rules are unsure."""
