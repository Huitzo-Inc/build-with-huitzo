# ============================================================================
# AUTO-GENERATED by huitzo CLI from huitzo.yaml — DO NOT EDIT THIS FILE
# All pack configuration belongs in huitzo.yaml
# Regenerate with: huitzo pack sync
# ============================================================================

[project]
name = "hello-pack"
version = "0.1.0"
description = "The smallest complete Intelligence Pack: text in, model call, typed output."
requires-python = ">=3.11"
dependencies = [
    "huitzo-sdk>=0.0.1",
]
authors = [{ name = "Huitzo" }]

[project.entry-points."huitzo.commands"]
"@reefs/hello-pack/hello" = "hello_pack.commands.hello:hello"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
sources = ["src"]

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]

[tool.mypy]
strict = true

[tool.ruff]
target-version = "py311"
line-length = 100
