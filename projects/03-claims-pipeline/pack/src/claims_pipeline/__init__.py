"""claims-pipeline: a Tier 3 example that composes three typed commands into one
governed pipeline. Each command is independently testable; the pipeline declared
in huitzo.yaml runs them in order and type-checks every handoff."""
